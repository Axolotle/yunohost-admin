- size: no 'xs'? override BButton sizes and add "xs" since it is present in scss
- app info form in config panel form
- replace CardCollapse by YCard collapsable + rm CardCollapse
<!-- - rm store + vuex -->
- rm ViewBase?
- rework skeleton (Skeleton list with search bar)
- use `defineModel` + rm `modelValue` prop + `update-model-value` event
- turn all `BAlert` to `YAlert`

- components.d.ts commit at the end
<!-- - pinia? -->



- merge dev
- update translation
- reformat translation (`@` escapted, `{c}` to `{n}` for pluralization)